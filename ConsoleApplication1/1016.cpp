#include "stdafx.h"
#include "iostream"
#include "string"
#include "regex"
#include "sstream"
#include<vector>
#include<algorithm>
#include <math.h>
#include <iomanip>
using namespace std;
/*int main()
{
	int a, b, c, d;
	int count1, count2,out1,out2;
	count1 = count2=out1=out2 = 0;
	scanf_s("%d %d %d %d", &a, &b, &c, &d);
	while (a != 0) {
		if (a%10 == b) {
			out1 = out1 + pow(10, count1)*b;
			count1++;
		}
		a /= 10;
	}
	while (c != 0) {
		if (c%10 == d) {
			out2 = out2 + pow(10, count2)*d;
			count2++;
		}
		c /= 10;
	}
	cout << int(out1 + out2);
	system("pause");
	return 0;
}
*/