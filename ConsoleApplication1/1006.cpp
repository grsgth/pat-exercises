#include "stdafx.h"
#include "iostream"
#include "string"
#include "regex"
#include "sstream"
#include<vector>
#include<algorithm>
/*int main()
{
	using namespace std;
	int format[3] = { 0 };
	int num;
	int i = 0;
	cin >> num;
	while (num != 0) {
		format[i]=num%10;
		num /= 10;
		i++;
	}
	for (int i = format[2]; i > 0; i--) {
		cout << "B";
	}
	for (int i = format[1]; i > 0; i--) {
		cout << "S";
	}
	for (int i = format[0]; i > 0; i--) {
		cout << format[0]-i+1;
	}
	system("pause");

	return 0;
}*/
