#include "stdafx.h"
#include "iostream"
#include "string"
#include "regex"
#include "sstream"
#include<vector>
#include<algorithm>
#include <math.h>
#include <iomanip>
using namespace std;
/*int main()
{
	int A, B, D;
	cin >> A >> B >> D;
	A = A + B;
	int jjg[1000] = { 0 };
	int count = 0;
	do {
		jjg[count++] = A%D;
		A /= D;

	} while (A != 0);
	for (count--; count >= 0; count--) {
		cout << jjg[count];
	}
	system("pause");
	return 0;
}*/
