#include "stdafx.h"
#include "iostream"
#include "string"
#include "regex"
#include "sstream"
#include<vector>
#include<algorithm>
#include <math.h>
/*int main()
{
	using namespace std;
	int n;
	string y;
	vector<long> num;
	cin >> n;
	for (int i = 0; i < n+1; i++) {
		getline(cin, y);
		stringstream input(y);
		while (input >> y) {
			num.push_back(stol(y));
		}
	}
	for (int i = 0; i < n; i++) {
		string bl;
		if (num[i*3] + num[i*3 + 1] > num[i*3 + 2]) {
			bl = "true";
		}
		else {
			bl = "false";
		}
		cout << "Case #" << i+1 << ": " << bl<<endl;
		
	}

	system("pause");
	return 0;
}*/
