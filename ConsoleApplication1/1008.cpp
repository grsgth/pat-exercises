#include "stdafx.h"
#include "iostream"
#include "string"
#include "regex"
#include "sstream"
#include<vector>
#include<algorithm>
#include <math.h>
using namespace std;
/*int main()
{
	void swap1(vector<int>& k, int a, int b);
	vector<int> count,num;
	string x,y,w,z;
	getline(cin, x);

	stringstream input(x);
	while (input >> y) {
		count.push_back(stoi(y));
	}
	getline(cin, w);
	stringstream input1(w);
	while (input1 >> z) {
		num.push_back(stoi(z));
	}
	if (count[1] > count[0])
		count[1] = count[0];
	swap1(num, 0, count[0]- count[1]-1);
	swap1(num, count[0] - count[1], count[0] - 1);
	swap1(num, 0, count[0] - 1);
	for (vector<int>::iterator it = num.begin(); it < num.end() - 1;it++) {
		cout << *it << " ";
	}
	cout << *(num.end() - 1);
	system("pause");
	return 0;
}

void swap1(vector<int>& k, int a, int b) {
	while (a < b) {
		swap(*(k.begin()+a),*(k.begin()+b));
		a++;
		b--;
	}
}
*/
