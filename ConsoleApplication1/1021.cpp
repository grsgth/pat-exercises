#include "stdafx.h"
#include "iostream"
#include "string"
#include "regex"
#include "sstream"
#include<vector>
#include<algorithm>
#include <math.h>
#include <iomanip>
using namespace std;
/*int main()
{
	string y;
	getline(cin, y);
	int count[10] = { 0 };
	for (int i = 0; i < y.length(); i++) {
		count[y[i] - '0']++;
	}
	for (int i = 0; i < 10; i++) {
		if (count[i] != 0)
			cout << i << ":" << count[i] << endl;
	}
	system("pause");
	return 0;
}*/
